/**
 * Extension temps réel messagerie (Socket.io / WS).
 * Stub intentionnel : la synchro repose sur le polling HTTP côté client.
 *
 * Intégration future :
 *   import { Server } from "socket.io";
 *   io.of("/messaging").use(authenticateSocket).on("connection", ...);
 */
export function attachMessagingSocket(_httpServer) {
  return null;
}
